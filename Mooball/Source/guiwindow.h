#ifndef	__GUIWINDOW_H_
#define	__GUIWINDOW_H_

#include "guiwidget.h"
#include "quickmath.h"

#include <string>


/*
class CGuiWindow : public CGuiWidget
{
	public:
		CGuiWindow();
		~CGuiWindow();

		void Initialize(vec2f& pos, vec2f& dim, std::string& caption, GuiColor& color);
		void Initialize(vec2f& pos, vec2f& dim, std::string& caption);
		void Render();
		void Dispose();

	private:
		std::string m_sCaption;
};
*/
#endif